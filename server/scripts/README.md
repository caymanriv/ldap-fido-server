# Scripts Directory

Helper scripts for development and operations.

- `seed-ldap.js` — Script to populate OpenLDAP with test users
- `generate-cert.sh` — Script to generate self-signed certificates for SAML
